'use strict';

const handleSequencePublish = require('../../src/clients/player1/handleSequencePublish');

describe('Tests for sequence publishing on player screen', () => {
  test.skip('Server publishing initial sequence, displays that sequence on player screen', () => {
    // Server publishing initial sequence

    // displays that sequence on player screen
  })
  test.skip('Server publishing a non-initial sequence (2nd/3rd/4th), displays that sequence on player screen', () => {
    // Server publishing a non-initial sequence (2nd/3rd/4th)

    // displays that sequence on player screen
  })
});