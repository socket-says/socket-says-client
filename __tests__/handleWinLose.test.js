'use strict';

const handleWinLose = require('../../src/clients/player1/handleWinLose');

describe('Tests for client finishing a game', () => {
  test('After client receives win/lose message, main-page prompt is displayed on client screen', () => {
    // After client receives win/lose message

    // expect(console.log).toEqual(''); // main-page prompt is displayed on client screen
  })
})